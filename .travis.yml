language: node_js
node_js:
  - "12"

cache:
  directories:
    - ~/.cmake-js/boost/

os:
  - linux
#  - osx

dist: xenial

before_install:
  - sudo apt-get install -y libgmp-dev

install:
  - npm install
  - npm run build

